Python 标准库 + 可选 LlamaIndex（`VectorStoreIndex`、`SentenceSplitter`、`Settings.embed_model`）与 FastEmbed（`intfloat/multilingual-e5-small` 及中文回退模型）；LLM 通过 HTTP 调用 Agnes AI Chat Completions API，无密钥时回退至内置 `MockLLM`；embedding 支持 SiliconFlow API (`BAAI/bge-m3`, 8192 token 上下文, 1024 维) 与本地 fastembed 双通道。