运行测试需确保模块所在目录在 `sys.path` 中（测试文件已 `sys.path.insert(0, ...)`）；启用真实 LLM 需在环境变量中设置 `AGNES_API_KEY`，否则自动降级为 `MockLLM`；启用 SiliconFlow embedding 需设置 `SILICONFLOW_API_KEY`；启用 RAGAS 自我修正需设置 `insight_self_correct=True` 及 `insight_quality_threshold`。