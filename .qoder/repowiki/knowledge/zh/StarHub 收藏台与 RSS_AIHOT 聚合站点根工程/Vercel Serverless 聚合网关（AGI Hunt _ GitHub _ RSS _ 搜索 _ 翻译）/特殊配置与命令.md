部署前需在 Vercel 环境变量中配置 GH_TOKEN（fine-grained PAT，Actions:write 权限）、AGIHUNT_API_KEY、REFRESH_KEY、AGNES_API_KEY；构建产物必须包含 rss_sources.json、rss_api_snapshot.json、translations.json 三个静态数据文件，供 rss.js 与 article.js 运行时读取；build_logs/ 目录需存在 .jsonl 日志文件以支持 build_log.js 查询。