根 `package.json` 仅声明构建期依赖，所有脚本通过 `node xxx.js` 或 `python fetch_and_build.py [full|incremental]` 运行；Vercel 部署配置集中在根 `vercel.json`，按函数路径声明 `maxDuration`；CI 通过 `update.yml` 定时任务驱动，使用 `--force-with-lease` 推送并带 5 次重试的 Vercel 部署。