构建入口为 `python fetch_and_build.py [full|incremental]`，需设置 `GITHUB_TOKEN` 或 `GH_TOKEN` 环境变量以访问 GitHub API；CI 中通过 `.github/workflows/update.yml` 在 UTC 21:00 执行 full 模式、UTC 2/6/10/14 执行 incremental 模式；本地模板调试使用 `python dev_render.py` 从 `index.html` 反推 `template.html`；Wiki 一致性校验运行 `python check_wiki_sync.py`。