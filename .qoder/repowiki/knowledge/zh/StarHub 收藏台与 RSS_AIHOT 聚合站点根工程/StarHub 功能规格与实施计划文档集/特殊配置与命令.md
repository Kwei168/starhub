本地预览流程：运行 `python dev_render.py` 重新渲染 `index.html`，再在 http://localhost:8923/index.html 浏览器检查；结构断言使用 python 单行命令校验占位符残留与关键类名存在性；Plan 中每个 Task 末尾附带 Run/Expected 形式的本地验证步骤（如 `python -m pytest test_insight_engine.py -v`）。