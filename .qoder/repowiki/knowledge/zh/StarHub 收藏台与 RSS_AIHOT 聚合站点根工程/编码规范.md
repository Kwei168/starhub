- API 端点以独立 `.js` 文件放在 `api/` 目录下，超时限制在根 `vercel.json` 中集中声明而非代码内硬编码
- 构建与数据抓取脚本使用纯 Node.js/Python 执行，不引入框架，依赖通过根 `package.json` 统一管理
- 前端页面由脚本动态生成，模板与数据分离，运行时只加载静态资源
- GitHub Actions 使用 `concurrency.cancel-in-progress: true` 保证同一项目永远只运行最新一次构建